<div class='container px-5 px-sm-0'>
  <div class="col text-center"><%= render 'searches/search' %></div>
  <div class='row'>
    <div class='col-md-8 offset-md-2'>
      <h2>下書き一覧</h2>
      <%= render 'index', books: @books %>
    </div>
  </div>
</div>