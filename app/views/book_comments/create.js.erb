$("#book_<%= @book.id %> .comments-count").html("コメント数: <%= @book.book_comments.count %>");
<!--id="comments_areaをr$("#enderで指定した部分テンプレートに更新-->
$("#comments_area").html("<%= j(render 'book_comments/index', book: @book, book_comment: @book_comment) %>");
<!--フォームを送信したあとにフォームの値を空にします-->
$("textarea").val("");